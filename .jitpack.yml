jdk:
  - openjdk11

android:
  components:
    - build-tools;33.0.0
    - platform-tools
    - platforms;android-33

env:
  GRADLE_USER_HOME: /home/jitpack/gradle_cache

build:
  script:
    - cd crates/kotlin-ffi/android
    - ./gradlew assembleRelease --no-daemon --refresh-dependencies --debug